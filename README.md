# MathVideo: 自动化数学视频生成器

一个利用大语言模型 (Kimi/GPT) 和 Manim 自动生成数学讲解视频的端到端系统。

## ✨ 核心特性

- **🤖 智能规划**: 使用 LLM 自动将数学主题拆解为结构化的分镜脚本
- **🎬 代码生成**: 自动生成 Manim Python 动画代码
- **🔧 自动修复**: 渲染失败时自动分析错误并修复代码
- **👁️ 视觉反馈**: 使用视觉大模型 (Qwen2.5-VL) 分析生成的视频帧，检测布局问题
- **✨ 智能优化**: 根据视觉反馈自动优化代码，提升视频质量
- **🖼️ 资产管理**: 自动识别并下载/生成所需的图标资源

## 📋 环境要求

- [Anaconda](https://www.anaconda.com/products/distribution) 或 [Miniconda](https://docs.conda.io/en/latest/miniconda.html)
- [FFmpeg](https://ffmpeg.org/) (Manim 必需)
- **LaTeX** (数学公式渲染必需)
    - **MacOS**: `brew install --cask mactex` 或安装 [MacTeX](https://www.tug.org/mactex/)
    - **Linux**: `sudo apt-get install texlive-full`
    - **Windows**: 安装 [MiKTeX](https://miktex.org/)

> **关于 LaTeX**: 系统内置了 LaTeX 回退机制，即使未安装 LaTeX 也能运行（使用 Text 替代 MathTex），但为获得最佳数学公式效果，建议安装完整的 LaTeX 发行版。

## 🚀 快速开始

### 1. 创建 Conda 环境

```bash
conda create -n mathvideo python=3.10 -y
conda activate mathvideo
```

### 2. 安装 Manim (推荐使用 conda-forge)

```bash
conda install -c conda-forge manim ffmpeg -y
```

### 3. 安装 Python 依赖

```bash
pip install -r requirements.txt
```

### 4. 配置 API 密钥

编辑 `src/config.py`:

```python
# 必填: Kimi API (用于代码生成)
KIMI_API_KEY = "your-kimi-api-key"

# 可选: Hugging Face API (用于视觉反馈)
HF_API_KEY = "your-huggingface-api-key"

# 可选: IconFinder API (用于资产下载)
ICONFINDER_API_KEY = "your-iconfinder-api-key"
```

## 📖 使用方法

### 基本用法

```bash
# 生成并渲染视频
python main.py "勾股定理" --render

# 仅生成脚本，不渲染
python main.py "圆的面积"
```

### 输出结构

```
output/
  勾股定理/
    storyboard.json       # 分镜脚本 (JSON)
    assets/               # 下载/生成的图标资源
      triangle.svg
      square.svg
    scripts/              # 生成的 Manim Python 脚本
      section_1.py
      section_2.py
      ...
    media/                # 渲染输出
      videos/
        section_1/
          480p15/
            frames/       # 视觉分析用的关键帧
            Section1Scene.mp4
```

## 🏗️ 系统架构

### 核心模块

| 模块 | 文件 | 功能 |
|------|------|------|
| **Planner** | `src/agents/planner.py` | 将主题转换为分镜脚本 |
| **Coder** | `src/agents/coder.py` | 生成 Manim 代码 |
| **Fixer** | `src/agents/coder.py` | 修复运行错误 |
| **Refiner** | `src/agents/coder.py` | 根据视觉反馈优化代码 |
| **Critic** | `src/agents/critic.py` | 视觉质量分析 |
| **AssetManager** | `src/agents/asset_manager.py` | 资产下载/生成 |
| **TeachingScene** | `src/manim_base.py` | Manim 基础类和网格系统 |

### 工作流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           MathVideo 生成流程                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  [输入]                                                                      │
│    │                                                                        │
│    ▼                                                                        │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                      │
│  │   Planner   │───▶│AssetManager │───▶│    Coder    │                      │
│  │ (分镜规划)   │    │ (资产准备)   │    │ (代码生成)  │                      │
│  └─────────────┘    └─────────────┘    └──────┬──────┘                      │
│                                               │                             │
│                                               ▼                             │
│                                        ┌─────────────┐                      │
│                                        │   Manim    │                      │
│                                        │  (渲染)    │                      │
│                                        └──────┬──────┘                      │
│                                               │                             │
│                              ┌────────────────┼────────────────┐            │
│                              │                │                │            │
│                              ▼                ▼                ▼            │
│                         [成功]           [失败]          [成功]            │
│                           │                │                │              │
│                           │                ▼                ▼              │
│                           │         ┌─────────────┐  ┌─────────────┐       │
│                           │         │   Fixer    │  │   Critic   │       │
│                           │         │ (错误修复)  │  │ (视觉分析)  │       │
│                           │         └──────┬──────┘  └──────┬──────┘       │
│                           │                │                │              │
│                           │                ▼                ▼              │
│                           │           [重新渲染]      ┌─────────────┐       │
│                           │                │         │  Refiner   │       │
│                           │                │         │ (代码优化)  │       │
│                           │                │         └──────┬──────┘       │
│                           │                │                │              │
│                           │                │                ▼              │
│                           │                │           [重新渲染]          │
│                           │                │                │              │
│                           ▼                ▼                ▼              │
│                        ┌─────────────────────────────────────┐             │
│                        │            输出视频                  │             │
│                        └─────────────────────────────────────┘             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🎯 TeachingScene 网格系统

所有生成的视频使用统一的布局框架：

```
┌───────────────────────────────────────────────────────────────┐
│ 标题                          │                               │
│ ─────────────────────         │  A1  A2  A3  ...  A10        │
│ 讲义笔记 1                     │  B1  B2  B3  ...  B10        │
│ 讲义笔记 2                     │  C1  C2  C3  ...  C10        │
│ 讲义笔记 3                     │  ...                         │
│ ...                           │  J1  J2  J3  ...  J10        │
└───────────────────────────────────────────────────────────────┘
        左侧 (讲义区)                    右侧 (动画区 10x10 网格)
```

### 定位方法

```python
# 单点定位
self.place_at_grid(circle, 'E5', scale_factor=0.8)

# 区域定位 (推荐用于几何图形)
self.place_in_area(triangle, 'B2', 'H8', scale_factor=0.9)

# 几何标签辅助方法
label = self.add_side_label(polygon, side_index=0, "a", color=BLUE)
label = self.add_vertex_label(polygon, vertex_index=0, "A")
mark = self.add_right_angle_mark(polygon, vertex_index=1)
```

## 🔧 配置选项

在 `src/config.py` 中可配置：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `USE_VISUAL_FEEDBACK` | 启用视觉反馈优化 | `True` |
| `KIMI_API_KEY` | Kimi API 密钥 | 必填 |
| `HF_API_KEY` | Hugging Face API 密钥 | 可选 |
| `HF_VISION_MODEL_NAME` | 视觉模型名称 | `Qwen/Qwen2.5-VL-72B-Instruct` |

## 📚 相关项目

本项目参考了 [Code2Video](https://github.com/example/code2video) 的部分设计理念。

## 📄 License

MIT License
